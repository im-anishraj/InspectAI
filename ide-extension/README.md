# InspectAI IDE Companion

This is the VS Code companion extension for InspectAI.

It listens on port 4444 for incoming WebSocket connections from the InspectAI Chrome Extension. When you click an element in your browser and type a prompt, this extension receives the payload, opens the correct file in VS Code, highlights the line, and opens the Agent Chat panel.
